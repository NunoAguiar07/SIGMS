/**
 * Interface of the error.
 * @param status represent the error code.
 * @param message represent the message of the error.
 */
export interface ErrorInterface {
    status: number;
    message: string;
}